# Only needed if NOT using OpenClaw's preconfigured model access
# ANTHROPIC_API_KEY=sk-ant-...

# OpenCode model configuration (optional — OpenClaw resolves automatically)
# OPENCODE_MODEL=zen/kimi-k2-5
# OPENCODE_FALLBACK_MODEL=auto

# WhatsApp
# BOT_NAME=WhatsApp Agent
# LOG_LEVEL=info
