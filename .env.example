# Inference provider selection
INFERENCE_PROVIDER=openrouter  # or 'anthropic', 'opencodezen', 'mistral'

# API keys (required based on provider choice)
ANTHROPIC_API_KEY=sk-ant-...   # only needed if INFERENCE_PROVIDER=anthropic
OPENROUTER_API_KEY=sk-or-...   # only needed if INFERENCE_PROVIDER=openrouter
OPENCODE_API_KEY=sk-op-...     # only needed if INFERENCE_PROVIDER=opencodezen
MISTRAL_API_KEY=sk-mis-...     # only needed if INFERENCE_PROVIDER=mistral

# OpenCode model configuration (optional — OpenClaw resolves automatically)
# OPENCODE_MODEL=zen/kimi-k2-5
# OPENCODE_FALLBACK_MODEL=auto

# WhatsApp
# BOT_NAME=WhatsApp Agent
# LOG_LEVEL=info